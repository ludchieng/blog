<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta charset="utf-8">
    <link rel="stylesheet" href="/assets/styles/main.css">
    <link rel="stylesheet" href="/assets/styles/index.css">
    <title>Index – ludchieng.fr</title>
  </head>
  <body>
    <header>
      <h1>ludchieng.fr</h1>
      <p class="subtitle">Be excellent to each other,<br /> Party on dudes.</p>
    </header>
    <main>
      {% for post in collections.posts.pages %}
        <h2>{{ post.title }}</h2>
        <p class="publication-date">Publié le {{ post.published_date | date: "%d/%m/%Y" }}</p>
        <p>{{ post.excerpt }}</p>
        <a href="{{ post.permalink }}">Lire l'article</a>
      {% endfor %}
    </main>
  </body>
</html>
